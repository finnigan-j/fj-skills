[Unit]
Description=Weekly backup of Claude Code config into a private git repo

[Service]
Type=oneshot
ExecStart=/bin/bash __REPO__/claude-backup/bin/claude-backup
# systemd user services do not read your shell rc; keep PATH explicit.
Environment=PATH=/usr/local/bin:/usr/bin:/bin
